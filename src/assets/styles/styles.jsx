import { StyleSheet } from "react-native";

export const mainStyles = StyleSheet.create({
    btn: {
        padding: 10,
        borderRadius: 5,
        backgroundColor: 'green',
        alignItems: 'center',
        color: 'white',
    },
});